<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <EditText
        android:id="@+id/edtProjectName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Tên đồ án (vd: Đồ án Quản lý deadline)"
        android:inputType="text"
        android:maxLines="1" />

    <EditText
        android:id="@+id/edtProjectDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:hint="Mô tả ngắn (tùy chọn)"
        android:inputType="textMultiLine"
        android:minLines="2" />

    <TextView
        android:id="@+id/tvPickDueDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:padding="12dp"
        android:background="#F2F2F2"
        android:text="Chọn hạn chót đồ án (tùy chọn)"
        android:textColor="#333333"
        android:gravity="center_vertical"
        android:clickable="true"
        android:focusable="true" />

</LinearLayout>
